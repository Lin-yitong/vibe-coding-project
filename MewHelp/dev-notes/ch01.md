# Ch01 开发过程

## Brainstorm 定稿（2026-09-13）

- **关键原话**：用户要求“用 Superpowers 模式做一个电商智能客服系统,第一步先跑通纯对话”，包含多轮 SSE、PromptTemplate、`with_structured_output`、token 预算裁剪，并明确本章不做工具调用与 Agent 循环。用户要求过程留痕、具体库 API 先查 Context7、技术选型不得自行替换。
- **关键产出**：设计 spec 写入 `docs/superpowers/specs/2026-09-13-mewhelp-ch01-pure-chat-design.md`。已确认采用 `MewHelp/` 为项目根目录、内存 `session_id` 会话、仅后端与 curl 验收、中文电商售后 System Prompt、LiteLLM proxy 双进程，以及硅基流动 API Key 作为当前上游。
- **用户纠偏/拒绝**：初次目录检查漏掉了空的 `MewHelp/`，用户指出“我当前不是建了吗”；已改为以 `MewHelp/` 开发。初版目录清单遗漏 `app/main.py`，用户追问 FastAPI 入口后已修正。用户拒绝 FastAPI 直连上游，明确选择 LiteLLM proxy 双进程方案。
- **翻车与返工**：文件枚举只列文件造成对空目录的误判；设计目录遗漏 FastAPI 入口，均已在设计中修正。Context7 MCP 已安装但 OAuth 回调报 issuer 缺失；进入具体库 API 实现前需在新 Codex task 中加载 Context7 工具并按官方文档核对。
